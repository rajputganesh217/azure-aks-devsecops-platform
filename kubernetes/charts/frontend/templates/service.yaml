apiVersion: v1
kind: Service
metadata:
  name: shoe-frontend
  labels:
    app: shoe-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: shoe-frontend
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
