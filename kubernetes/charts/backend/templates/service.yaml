apiVersion: v1
kind: Service
metadata:
  name: shoe-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: shoe-backend
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
